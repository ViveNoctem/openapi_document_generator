name: shelf_router_generator_example
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.13.0
resolution:
  workspace

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2

dev_dependencies:
  build_runner: ^2.16.1
  http: ^1.2.2
  lints: ^6.0.0
  shelf_router_generator: ^1.1.5
  test: ^1.25.6
