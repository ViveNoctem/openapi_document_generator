name: openapi_document_annotation
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.13.0
resolution: workspace

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  build_runner: ^2.16.1
  lints: ^6.0.0
  openapi_document_generator: 
  test: ^1.25.6
